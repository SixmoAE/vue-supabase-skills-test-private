import { AlertCircle, CheckCircle, Info, XCircle } from "lucide-vue-next";

export const toastIcons = {
    success: CheckCircle,
    error: XCircle,
    warning: AlertCircle,
    info: Info,
    default: null,
};
